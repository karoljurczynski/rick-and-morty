interface PaginationTileWrapperProps {
  ellipsis?: boolean;
  active?: boolean;
}

interface PaginationTileProps {
  prevButton?: boolean;
  nextButton?: boolean;
  pageNumber?: number;
  ellipsis?: boolean;
  active?: boolean;
}